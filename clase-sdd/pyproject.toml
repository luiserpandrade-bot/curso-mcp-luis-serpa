[project]
name = "clase-sdd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Luis Serpa", email = "luiserpandrade@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
clase-sdd = "clase_sdd:main"
convertidor-temperatura = "clase_sdd:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
