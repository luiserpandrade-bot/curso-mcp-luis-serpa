[project]
name = "curso-mcp-luis-serpa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=2.22.0",
    "python-dotenv>=1.2.3",
    "rich>=15.0.0",
]

[project.scripts]
curso-mcp-luis-serpa = "curso_mcp_luis_serpa:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
